export const publicRoutes: string[] = ['/new-verification'];

export const authRoutes: string[] = [
  '/signup',
  '/login',
  '/password-reset',
  '/new-password',
];

export const apiAuthPrefix: string = '/api/auth';

export const DEFAULT_LOGIN_REDIRECT = '/outputs';
